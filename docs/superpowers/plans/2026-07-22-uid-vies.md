# `@kreiseck/uid` (VIES-first) + `@kreiseck/finanzonline` (DataBox) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Robuste EU-USt-ID-Validierung als `@kreiseck/uid` (VIES-first, FON-UID sekundär) und ein minimaler DataBox-Client `@kreiseck/finanzonline` für den FON-UID-Bescheid.

**Architecture:** Zwei neue npm-Workspace-Pakete im Monorepo. `@kreiseck/uid` prüft primär über die EU-VIES-REST-API (global `fetch`, keyless) und sekundär über FinanzOnline-UID (SOAP via `@kreiseck/finanzonline-core`). Einheitliches 3-Ausgänge-Ergebnismodell. `@kreiseck/finanzonline` kapselt die DataBox-Operationen (`getDatabox`/`getDataboxEntry`) über core. Alles zustandslos.

**Tech Stack:** Node ≥20.18.0, TypeScript strict, CJS via `tsc`, `node:test`, keine Laufzeitabhängigkeiten außer `@kreiseck/finanzonline-core`.

**Spec:** [docs/superpowers/specs/2026-07-22-uid-vies-design.md](../specs/2026-07-22-uid-vies-design.md) · **Fakten:** [docs/research/2026-07-22-finanzonline-webservices.md](../../research/2026-07-22-finanzonline-webservices.md)

## Global Constraints
- Node `>=20.18.0`; `@types/node` `^22`; TS `strict` + `noUncheckedIndexedAccess` + `noImplicitAny`; Build `tsc` → CJS; Tests `node:test` via `tsc -p tsconfig.test.json && node --test`.
- **Keine Laufzeitabhängigkeiten** außer `@kreiseck/finanzonline-core` `^0.1.2` (nur `node:*` + globales `fetch`). Zustandslos, framework-agnostisch.
- Lizenz **Apache-2.0**; `NOTICE` je Paket; `publishConfig.access: public`; `files: ["dist","NOTICE"]`.
- **Deutsche öffentliche API.** Kein KI-/Tool-Hinweis in Code/Kommentaren/Commits.
- `@kreiseck/uid` hängt **nicht** an `@kreiseck/finanzonline`.
- Returncodes/Feldnamen/Endpoints WÖRTLICH aus dem Research-Dokument. Werte in den Tasks sind daraus übernommen.
- ESLint/Prettier am Repo-Root müssen grün bleiben (`npm run lint`, `npm run format:check`).

## File Structure
```
packages/uid/
  package.json  tsconfig.json  tsconfig.test.json  NOTICE  README.md
  src/
    index.ts          # öffentliche Exports
    ergebnis.ts       # UidErgebnis, Ausgang, Nachweis, KeinAntwortGrund, Fehlertypen
    normalisieren.ts  # UID normalisieren + land/nummer-Split
    vies.ts           # VIES-REST: pruefe/bestaetige/viesStatus + Ausgang-Mapping
    fon.ts            # FON-UID: Envelope, Parse, Returncode→Ausgang
    uid.ts            # createUid Factory (komponiert vies+fon, cacheKey)
packages/finanzonline/
  package.json  tsconfig.json  tsconfig.test.json  NOTICE  README.md
  src/
    index.ts          # öffentliche Exports
    databox.ts        # createDatabox: liste/eintrag
```
Referenzmuster für Paket-Boilerplate: `packages/rksv/package.json`, `packages/rksv/tsconfig*.json`, `packages/finanzonline-core/src/session.ts` (Envelope/callSoap-Nutzung).

---

## Task 1: `@kreiseck/uid` Paket-Gerüst

**Files:**
- Create: `packages/uid/package.json`, `packages/uid/tsconfig.json`, `packages/uid/tsconfig.test.json`, `packages/uid/NOTICE`, `packages/uid/src/index.ts`

**Interfaces:**
- Produces: ein baubares, leeres Paket `@kreiseck/uid@0.1.0`, das `@kreiseck/finanzonline-core` auflöst.

- [ ] **Step 1: package.json** (spiegelt `packages/rksv/package.json`, ohne `./code`-Export)

```json
{
  "name": "@kreiseck/uid",
  "version": "0.1.0",
  "description": "EU-USt-ID-Validierung (VIES) und FinanzOnline-UID-Abfrage",
  "license": "Apache-2.0",
  "author": "Kreiseck",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist", "NOTICE"],
  "engines": { "node": ">=20.18.0" },
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "tsc -p tsconfig.test.json && node --test $(find test-dist -name '*.test.js')",
    "prepublishOnly": "npm run build"
  },
  "dependencies": { "@kreiseck/finanzonline-core": "^0.1.2" },
  "devDependencies": { "typescript": "^5.4.0", "@types/node": "^22.0.0" }
}
```

- [ ] **Step 2: tsconfig.json + tsconfig.test.json** — inhaltlich identisch zu `packages/rksv/tsconfig.json` bzw. `tsconfig.test.json` (gleiche compilerOptions: strict, noUncheckedIndexedAccess, outDir dist bzw. test-dist, module commonjs, target ES2022). Kopiere die Dateien 1:1 und passe nur enthaltene Pfade an, falls nötig.

- [ ] **Step 3: NOTICE** — Kopie von `packages/rksv/NOTICE`.

- [ ] **Step 4: src/index.ts** (vorläufig leer, damit der Build durchläuft)
```ts
export {};
```

- [ ] **Step 5: Build prüfen**
Run: `npm install && npm run build -w @kreiseck/uid`
Expected: kein Fehler; `packages/uid/dist/index.js` existiert.

- [ ] **Step 6: Commit**
```bash
git add packages/uid && git commit -m "feat(uid): Paket-Geruest @kreiseck/uid"
```

---

## Task 2: UID-Normalisierung + Land/Nummer-Split

**Files:**
- Create: `packages/uid/src/normalisieren.ts`, `packages/uid/src/normalisieren.test.ts`

**Interfaces:**
- Produces: `normalisiereUid(roh: string): { land: string; nummer: string; voll: string }`; wirft `UidEingabeError` bei ungültigem Format. Ländercodes VAT-Konvention (`EL`,`XI`). Pattern (Research): `(AT|BE|BG|CY|CZ|DE|DK|EE|EL|ES|FI|FR|GB|HR|HU|IE|IT|LT|LU|LV|MT|NL|PL|PT|SE|SI|SK|XI)[A-Za-z0-9]{8,12}|RO\d{2,10}`.

- [ ] **Step 1: Failing test**
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { normalisiereUid } from './normalisieren';
import { UidEingabeError } from './ergebnis';

test('normalisiert Leerzeichen/Kleinschreibung und splittet Land/Nummer', () => {
  const r = normalisiereUid(' de 136695976 ');
  assert.equal(r.land, 'DE');
  assert.equal(r.nummer, '136695976');
  assert.equal(r.voll, 'DE136695976');
});
test('AT-UID', () => {
  assert.deepEqual(normalisiereUid('ATU12345678'), { land: 'AT', nummer: 'U12345678', voll: 'ATU12345678' });
});
test('RO mit kurzer Ziffernnummer erlaubt', () => {
  assert.equal(normalisiereUid('RO1234').voll, 'RO1234');
});
test('unbekanntes Land wirft', () => {
  assert.throws(() => normalisiereUid('ZZ12345678'), UidEingabeError);
});
test('zu kurz wirft', () => {
  assert.throws(() => normalisiereUid('DE12'), UidEingabeError);
});
```

- [ ] **Step 2: Test schlägt fehl (Module fehlen)** — Run: `npm test -w @kreiseck/uid` → FAIL (ergebnis/normalisieren fehlen). `ergebnis.ts` wird in Task 3 vollständig; hier nur `UidEingabeError` minimal anlegen.

- [ ] **Step 3: Minimal `ergebnis.ts` (nur Fehlertyp, Rest folgt in Task 3)**
```ts
export class UidEingabeError extends Error {
  constructor(message: string) { super(message); this.name = 'UidEingabeError'; }
}
```

- [ ] **Step 4: Implementierung `normalisieren.ts`**
```ts
import { UidEingabeError } from './ergebnis';

const LAENDER = ['AT','BE','BG','CY','CZ','DE','DK','EE','EL','ES','FI','FR','GB','HR','HU','IE','IT','LT','LU','LV','MT','NL','PL','PT','SE','SI','SK','XI'] as const;

export function normalisiereUid(roh: string): { land: string; nummer: string; voll: string } {
  if (typeof roh !== 'string') throw new UidEingabeError('UID muss ein String sein');
  const voll = roh.replace(/[\s.-]/g, '').toUpperCase();
  const land = voll.slice(0, 2);
  const nummer = voll.slice(2);
  const gueltig =
    (LAENDER.includes(land as (typeof LAENDER)[number]) && /^[A-Z0-9]{8,12}$/.test(nummer)) ||
    (land === 'RO' && /^\d{2,10}$/.test(nummer));
  if (!gueltig) throw new UidEingabeError(`UID-Format ungültig: ${roh}`);
  return { land, nummer, voll };
}
```

- [ ] **Step 5: Test grün** — Run: `npm test -w @kreiseck/uid` → PASS.

- [ ] **Step 6: Commit** — `git commit -am "feat(uid): UID-Normalisierung + Land/Nummer-Split"`

---

## Task 3: Ergebnismodell + Ausgang-Klassifizierung

**Files:**
- Modify: `packages/uid/src/ergebnis.ts`
- Create: `packages/uid/src/ergebnis.test.ts`

**Interfaces:**
- Produces (aus Spec):
```ts
export type Ausgang = 'gueltig' | 'ungueltig' | 'keine_antwort';
export type KeinAntwortGrund = 'ms_nicht_erreichbar'|'timeout'|'ueberlast'|'wartung'|'ratenlimit'|'gesperrt'|'technisch'|'nicht_berechtigt';
export interface Nachweis { art: 'vies-konsultationsnummer'|'fon-bescheid-in-databox'; id?: string; datum: string; hinweis?: string; }
export interface UidErgebnis {
  ergebnis: Ausgang; quelle: 'vies'|'fon'; uid: string; land: string; abfragedatum: string;
  name?: string; adresse?: string; nachweis?: Nachweis;
  grund?: KeinAntwortGrund; wiederholbar?: boolean; rohRc?: string;
}
export class UidEingabeError extends Error {}
```
- Produces Helper `viesUserErrorAusgang(userError: string): { ergebnis: Ausgang; grund?: KeinAntwortGrund; wiederholbar?: boolean }` — mappt VIES-`userError`/Fault-Token (Research §VIES). `VALID`→gueltig; `INVALID`→ungueltig; `INVALID_INPUT`→(Aufrufer wirft); alle transienten (`MS_UNAVAILABLE`,`TIMEOUT`,`SERVICE_UNAVAILABLE`,`MS_MAX_CONCURRENT_REQ`,`GLOBAL_MAX_CONCURRENT_REQ`,`SERVER_BUSY`,`IO_ERROR`,`TECHNICAL_ERROR`)→keine_antwort+Grund+wiederholbar:true; `VAT_BLOCKED`/`IP_BLOCKED`→keine_antwort/gesperrt/wiederholbar:false.

- [ ] **Step 1: Failing test** (`ergebnis.test.ts`)
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { viesUserErrorAusgang } from './ergebnis';

test('VALID -> gueltig', () => assert.equal(viesUserErrorAusgang('VALID').ergebnis, 'gueltig'));
test('INVALID -> ungueltig', () => assert.equal(viesUserErrorAusgang('INVALID').ergebnis, 'ungueltig'));
test('MS_UNAVAILABLE -> keine_antwort, wiederholbar', () => {
  const a = viesUserErrorAusgang('MS_UNAVAILABLE');
  assert.equal(a.ergebnis, 'keine_antwort');
  assert.equal(a.grund, 'ms_nicht_erreichbar');
  assert.equal(a.wiederholbar, true);
});
test('MS_MAX_CONCURRENT_REQ -> keine_antwort/ueberlast, wiederholbar', () => {
  assert.equal(viesUserErrorAusgang('MS_MAX_CONCURRENT_REQ').grund, 'ueberlast');
});
test('IP_BLOCKED -> keine_antwort/gesperrt, NICHT wiederholbar', () => {
  const a = viesUserErrorAusgang('IP_BLOCKED');
  assert.equal(a.grund, 'gesperrt');
  assert.equal(a.wiederholbar, false);
});
```

- [ ] **Step 2: FAIL** — Run: `npm test -w @kreiseck/uid`.

- [ ] **Step 3: `ergebnis.ts` vervollständigen** — Typen (oben) + Mapping:
```ts
const TRANSIENT: Record<string, KeinAntwortGrund> = {
  MS_UNAVAILABLE: 'ms_nicht_erreichbar', SERVICE_UNAVAILABLE: 'wartung', TIMEOUT: 'timeout',
  MS_MAX_CONCURRENT_REQ: 'ueberlast', GLOBAL_MAX_CONCURRENT_REQ: 'ueberlast', SERVER_BUSY: 'ueberlast',
  IO_ERROR: 'technisch', TECHNICAL_ERROR: 'technisch',
};
export function viesUserErrorAusgang(userError: string) {
  if (userError === 'VALID') return { ergebnis: 'gueltig' as const };
  if (userError === 'INVALID') return { ergebnis: 'ungueltig' as const };
  if (userError === 'VAT_BLOCKED' || userError === 'IP_BLOCKED')
    return { ergebnis: 'keine_antwort' as const, grund: 'gesperrt' as const, wiederholbar: false };
  const grund = TRANSIENT[userError];
  if (grund) return { ergebnis: 'keine_antwort' as const, grund, wiederholbar: true };
  // Unbekannt: konservativ als keine_antwort behandeln (nie faelschlich ungueltig)
  return { ergebnis: 'keine_antwort' as const, grund: 'technisch' as const, wiederholbar: true };
}
```

- [ ] **Step 4: PASS** — Run: `npm test -w @kreiseck/uid`.
- [ ] **Step 5: Commit** — `git commit -am "feat(uid): Ergebnismodell + VIES-Ausgang-Klassifizierung"`

---

## Task 4: VIES `pruefe` (einfache Prüfung, REST GET)

**Files:**
- Create: `packages/uid/src/vies.ts`, `packages/uid/src/vies.test.ts`

**Interfaces:**
- Consumes: `normalisiereUid`, `viesUserErrorAusgang`, `UidErgebnis`, `UidEingabeError`.
- Produces: `interface ViesConfig { basis?: string; fetchImpl?: typeof fetch; }` und `viesPruefe(uid: string, cfg?: ViesConfig): Promise<UidErgebnis>`. Endpoint (Research): `GET {basis}/rest-api/ms/{land}/vat/{nummer}`. Antwortfelder: `isValid, userError, name, address, requestDate`. `INVALID_INPUT` → `UidEingabeError` werfen. Netzwerk-/Timeout-Fehler → `keine_antwort`/`timeout`/wiederholbar.

- [ ] **Step 1: Failing test** (injizierter `fetchImpl`)
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { viesPruefe } from './vies';

const jsonFetch = (body: unknown) =>
  (async () => new Response(JSON.stringify(body), { status: 200 })) as unknown as typeof fetch;

test('gueltige DE-UID -> gueltig, quelle vies, land DE', async () => {
  const erg = await viesPruefe('DE136695976', {
    fetchImpl: jsonFetch({ isValid: true, userError: 'VALID', name: '', address: '', requestDate: '2026-07-22' }),
  });
  assert.equal(erg.ergebnis, 'gueltig');
  assert.equal(erg.quelle, 'vies');
  assert.equal(erg.land, 'DE');
});
test('nicht registriert -> ungueltig', async () => {
  const erg = await viesPruefe('DE000000000', { fetchImpl: jsonFetch({ isValid: false, userError: 'INVALID' }) });
  assert.equal(erg.ergebnis, 'ungueltig');
});
test('MS_UNAVAILABLE -> keine_antwort/wiederholbar', async () => {
  const erg = await viesPruefe('DE136695976', { fetchImpl: jsonFetch({ isValid: false, userError: 'MS_UNAVAILABLE' }) });
  assert.equal(erg.ergebnis, 'keine_antwort');
  assert.equal(erg.wiederholbar, true);
});
test('Netzwerkfehler -> keine_antwort/timeout', async () => {
  const erg = await viesPruefe('DE136695976', {
    fetchImpl: (async () => { throw new Error('network'); }) as unknown as typeof fetch,
  });
  assert.equal(erg.ergebnis, 'keine_antwort');
  assert.equal(erg.grund, 'timeout');
});
```

- [ ] **Step 2: FAIL** — Run: `npm test -w @kreiseck/uid`.

- [ ] **Step 3: Implementierung `vies.ts`**
```ts
import { normalisiereUid } from './normalisieren';
import { viesUserErrorAusgang, UidEingabeError, type UidErgebnis } from './ergebnis';

export interface ViesConfig { basis?: string; fetchImpl?: typeof fetch; }
const DEFAULT_BASIS = 'https://ec.europa.eu/taxation_customs/vies';

export async function viesPruefe(uid: string, cfg: ViesConfig = {}): Promise<UidErgebnis> {
  const { land, nummer, voll } = normalisiereUid(uid);
  const f = cfg.fetchImpl ?? fetch;
  const basis = cfg.basis ?? DEFAULT_BASIS;
  const datum = new Date().toISOString();
  let data: { isValid?: boolean; userError?: string; name?: string; address?: string };
  try {
    const res = await f(`${basis}/rest-api/ms/${land}/vat/${nummer}`, { headers: { Accept: 'application/json' } });
    data = (await res.json()) as typeof data;
  } catch {
    return { ergebnis: 'keine_antwort', quelle: 'vies', uid: voll, land, abfragedatum: datum, grund: 'timeout', wiederholbar: true };
  }
  const userError = data.userError ?? (data.isValid ? 'VALID' : 'INVALID');
  if (userError === 'INVALID_INPUT') throw new UidEingabeError(`VIES: ungültige Eingabe für ${voll}`);
  const a = viesUserErrorAusgang(userError);
  const erg: UidErgebnis = { ...a, quelle: 'vies', uid: voll, land, abfragedatum: datum, rohRc: userError };
  if (a.ergebnis === 'gueltig') { if (data.name) erg.name = data.name; if (data.address) erg.adresse = data.address; }
  return erg;
}
```

- [ ] **Step 4: PASS** — Run: `npm test -w @kreiseck/uid`.
- [ ] **Step 5: Commit** — `git commit -am "feat(uid): VIES pruefe (REST) mit 3-Ausgaenge-Mapping"`

---

## Task 5: VIES `bestaetige` (qualifiziert, POST + Konsultationsnummer)

**Files:**
- Modify: `packages/uid/src/vies.ts`; Create tests in `packages/uid/src/vies-bestaetige.test.ts`

**Interfaces:**
- Produces: `viesBestaetige(args: { uid: string; antragsteller: string; name?: string; strasse?: string; plz?: string; ort?: string }, cfg?: ViesConfig): Promise<UidErgebnis & { matches?: Record<'name'|'strasse'|'plz'|'ort','match'|'kein_match'|'nicht_geprueft'> }>`. Endpoint `POST {basis}/rest-api/check-vat-number`; Body enthält `countryCode,vatNumber,requesterMemberStateCode,requesterNumber,traderName,traderStreet,traderPostalCode,traderCity`. Response zusätzlich `requestIdentifier` + `viesApproximate.match*` (1=match,2=kein_match,3=nicht_geprueft). Bei `gueltig` + vorhandener `requestIdentifier` → `nachweis`.
- **Offener Punkt (Spec #1):** exakte POST-Feldnamen live gegenprüfen; Task nutzt die dokumentierten Namen, im Code als benannte Konstanten kapseln.

- [ ] **Step 1: Failing test**
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { viesBestaetige } from './vies';

const jsonFetch = (body: unknown, capture?: (init: RequestInit) => void) =>
  (async (_u: unknown, init?: RequestInit) => { capture?.(init ?? {}); return new Response(JSON.stringify(body), { status: 200 }); }) as unknown as typeof fetch;

test('gueltig mit Konsultationsnummer + Matches', async () => {
  let sent: any;
  const erg = await viesBestaetige(
    { uid: 'DE136695976', antragsteller: 'ATU12345678', name: 'ACME', ort: 'Berlin' },
    { fetchImpl: jsonFetch({ isValid: true, userError: 'VALID', requestIdentifier: 'WAPIAAAAX1', viesApproximate: { matchName: 1, matchStreet: 3, matchPostalCode: 3, matchCity: 1 } }, (i) => { sent = JSON.parse(String(i.body)); }) },
  );
  assert.equal(erg.ergebnis, 'gueltig');
  assert.equal(erg.nachweis?.art, 'vies-konsultationsnummer');
  assert.equal(erg.nachweis?.id, 'WAPIAAAAX1');
  assert.equal(erg.matches?.name, 'match');
  assert.equal(erg.matches?.ort, 'match');
  assert.equal(erg.matches?.strasse, 'nicht_geprueft');
  // Antragsteller landet im Request:
  assert.equal(sent.requesterMemberStateCode, 'AT');
  assert.equal(sent.requesterNumber, 'U12345678');
});
```

- [ ] **Step 2: FAIL** — Run: `npm test -w @kreiseck/uid`.

- [ ] **Step 3: Implementierung ergänzen in `vies.ts`**
```ts
import { normalisiereUid as _n } from './normalisieren';
const MATCH: Record<number, 'match'|'kein_match'|'nicht_geprueft'> = { 1: 'match', 2: 'kein_match', 3: 'nicht_geprueft' };

export async function viesBestaetige(
  args: { uid: string; antragsteller: string; name?: string; strasse?: string; plz?: string; ort?: string },
  cfg: ViesConfig = {},
): Promise<UidErgebnis & { matches?: Record<'name'|'strasse'|'plz'|'ort','match'|'kein_match'|'nicht_geprueft'> }> {
  const ziel = normalisiereUid(args.uid);
  const anb = normalisiereUid(args.antragsteller);
  const f = cfg.fetchImpl ?? fetch;
  const basis = cfg.basis ?? DEFAULT_BASIS;
  const datum = new Date().toISOString();
  const body = {
    countryCode: ziel.land, vatNumber: ziel.nummer,
    requesterMemberStateCode: anb.land, requesterNumber: anb.nummer,
    traderName: args.name, traderStreet: args.strasse, traderPostalCode: args.plz, traderCity: args.ort,
  };
  let data: any;
  try {
    const res = await f(`${basis}/rest-api/check-vat-number`, { method: 'POST', headers: { 'Content-Type': 'application/json', Accept: 'application/json' }, body: JSON.stringify(body) });
    data = await res.json();
  } catch {
    return { ergebnis: 'keine_antwort', quelle: 'vies', uid: ziel.voll, land: ziel.land, abfragedatum: datum, grund: 'timeout', wiederholbar: true };
  }
  const userError = data.userError ?? (data.isValid ? 'VALID' : 'INVALID');
  if (userError === 'INVALID_INPUT') throw new UidEingabeError(`VIES: ungültige Eingabe für ${ziel.voll}`);
  const a = viesUserErrorAusgang(userError);
  const erg: UidErgebnis & { matches?: any } = { ...a, quelle: 'vies', uid: ziel.voll, land: ziel.land, abfragedatum: datum, rohRc: userError };
  if (data.name) erg.name = data.name; if (data.address) erg.adresse = data.address;
  const ap = data.viesApproximate;
  if (ap) erg.matches = { name: MATCH[ap.matchName] ?? 'nicht_geprueft', strasse: MATCH[ap.matchStreet] ?? 'nicht_geprueft', plz: MATCH[ap.matchPostalCode] ?? 'nicht_geprueft', ort: MATCH[ap.matchCity] ?? 'nicht_geprueft' };
  if (a.ergebnis === 'gueltig' && data.requestIdentifier) erg.nachweis = { art: 'vies-konsultationsnummer', id: data.requestIdentifier, datum };
  return erg;
}
```

- [ ] **Step 4: PASS** — Run: `npm test -w @kreiseck/uid`.
- [ ] **Step 5: Commit** — `git commit -am "feat(uid): VIES bestaetige (checkVatApprox) mit Konsultationsnummer"`

---

## Task 6: VIES `viesStatus` (Verfügbarkeit pro Mitgliedstaat)

**Files:**
- Modify: `packages/uid/src/vies.ts`; Create `packages/uid/src/vies-status.test.ts`

**Interfaces:**
- Produces: `viesStatus(cfg?: ViesConfig): Promise<{ vowVerfuegbar: boolean; land: Record<string,'verfuegbar'|'nicht_verfuegbar'|'beobachtet'> }>`. Endpoint `GET {basis}/rest-api/check-status`; `{vow:{available}, countries:[{countryCode, availability}]}`; `Available`→verfuegbar, `Monitored`→beobachtet, sonst nicht_verfuegbar.

- [ ] **Step 1: Failing test**
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { viesStatus } from './vies';
const jsonFetch = (b: unknown) => (async () => new Response(JSON.stringify(b), { status: 200 })) as unknown as typeof fetch;

test('mappt Verfuegbarkeit pro Land', async () => {
  const s = await viesStatus({ fetchImpl: jsonFetch({ vow: { available: true }, countries: [ { countryCode: 'DE', availability: 'Available' }, { countryCode: 'FR', availability: 'Unavailable' }, { countryCode: 'IT', availability: 'Monitored' } ] }) });
  assert.equal(s.vowVerfuegbar, true);
  assert.equal(s.land.DE, 'verfuegbar');
  assert.equal(s.land.FR, 'nicht_verfuegbar');
  assert.equal(s.land.IT, 'beobachtet');
});
```

- [ ] **Step 2: FAIL**, then **Step 3: Implementierung**
```ts
export async function viesStatus(cfg: ViesConfig = {}) {
  const f = cfg.fetchImpl ?? fetch;
  const basis = cfg.basis ?? DEFAULT_BASIS;
  const res = await f(`${basis}/rest-api/check-status`, { headers: { Accept: 'application/json' } });
  const data = (await res.json()) as { vow?: { available?: boolean }; countries?: Array<{ countryCode: string; availability: string }> };
  const land: Record<string, 'verfuegbar'|'nicht_verfuegbar'|'beobachtet'> = {};
  for (const c of data.countries ?? []) land[c.countryCode] = c.availability === 'Available' ? 'verfuegbar' : c.availability === 'Monitored' ? 'beobachtet' : 'nicht_verfuegbar';
  return { vowVerfuegbar: data.vow?.available === true, land };
}
```
- [ ] **Step 4: PASS** · **Step 5: Commit** — `git commit -am "feat(uid): viesStatus (check-status)"`

---

## Task 7: FON-UID `fon.abfrage` (SOAP über core)

**Files:**
- Create: `packages/uid/src/fon.ts`, `packages/uid/src/fon.test.ts`

**Interfaces:**
- Consumes aus core: `buildEnvelope`, `callSoap`, `findDescendant`, `childText`, `type Session`, `type TransportOptions`, `sessionErrorFor`.
- Produces: `fonUidAbfrage(args: { session: Session; antragsteller: string; uid: string; stufe: 1|2; transport?: TransportOptions }): Promise<UidErgebnis>`.
- Endpoint (Research): `https://finanzonline.bmf.gv.at/fonuid/ws/uidAbfrage/`, SOAPAction `uidAbfrage`, Namespace `https://finanzonline.bmf.gv.at/fon/ws/uidAbfrage`, Body-Element `uidAbfrageServiceRequest`, Felder in Reihenfolge `tid,benid,id,uid_tn,uid,stufe`. Response-Element `uidAbfrageServiceResponse`: `rc(int),msg?,name?,adrz1..adrz6?`.
- Returncode→Ausgang (Research-Tabelle):
```
0→gueltig · 1→ungueltig · 4/5→UidEingabeError werfen
-2,12,1511,1512→keine_antwort transient(wiederholbar:true; Grund: wartung/ms_nicht_erreichbar/ueberlast/ueberlast)
1513,1514→keine_antwort/ratenlimit(wiederholbar:false)
10,11,-4→keine_antwort/nicht_berechtigt(wiederholbar:false)
103,104,105→keine_antwort/nicht_berechtigt + hinweis (nur Stufe1/UI)
```
core wirft bei rc `-1` bereits `FonSessionExpiredError` (nicht hier behandeln).

- [ ] **Step 1: Failing test** (injizierter Transport mit Fixtures)
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { fonUidAbfrage } from './fon';
import { UidEingabeError } from './ergebnis';
import type { Session } from '@kreiseck/finanzonline-core';

const s: Session = { id: 'ABCDEFGHIJ1234567890', tid: 'ABCD1234', benid: 'benutzer1', async logout() {} };
const resp = (inner: string) => `<S:Envelope xmlns:S="http://schemas.xmlsoap.org/soap/envelope/"><S:Body><ns:uidAbfrageServiceResponse xmlns:ns="https://finanzonline.bmf.gv.at/fon/ws/uidAbfrage">${inner}</ns:uidAbfrageServiceResponse></S:Body></S:Envelope>`;
const tp = (xml: string) => ({ fetchImpl: (async () => new Response(xml, { status: 200 })) as unknown as typeof fetch });

test('rc 0 Stufe 2 -> gueltig + Name/Adresse', async () => {
  const erg = await fonUidAbfrage({ session: s, antragsteller: 'ATU12345678', uid: 'ATU87654321', stufe: 2, transport: tp(resp('<rc>0</rc><name>ACME GmbH</name><adrz1>Hauptstr 1</adrz1><adrz2>1010 Wien</adrz2>')) });
  assert.equal(erg.ergebnis, 'gueltig'); assert.equal(erg.quelle, 'fon');
  assert.equal(erg.name, 'ACME GmbH'); assert.match(erg.adresse ?? '', /Hauptstr 1/);
  assert.equal(erg.nachweis?.art, 'fon-bescheid-in-databox');
});
test('rc 1 -> ungueltig', async () => {
  const erg = await fonUidAbfrage({ session: s, antragsteller: 'ATU12345678', uid: 'ATU00000000', stufe: 1, transport: tp(resp('<rc>1</rc>')) });
  assert.equal(erg.ergebnis, 'ungueltig');
});
test('rc 1511 -> keine_antwort/wiederholbar', async () => {
  const erg = await fonUidAbfrage({ session: s, antragsteller: 'ATU12345678', uid: 'DE136695976', stufe: 2, transport: tp(resp('<rc>1511</rc>')) });
  assert.equal(erg.ergebnis, 'keine_antwort'); assert.equal(erg.wiederholbar, true);
});
test('rc 1513 -> keine_antwort/ratenlimit, NICHT wiederholbar', async () => {
  const erg = await fonUidAbfrage({ session: s, antragsteller: 'ATU12345678', uid: 'DE136695976', stufe: 2, transport: tp(resp('<rc>1513</rc>')) });
  assert.equal(erg.grund, 'ratenlimit'); assert.equal(erg.wiederholbar, false);
});
test('rc 4 -> UidEingabeError', async () => {
  await assert.rejects(() => fonUidAbfrage({ session: s, antragsteller: 'ATU12345678', uid: 'XX', stufe: 1, transport: tp(resp('<rc>4</rc>')) }), UidEingabeError);
});
```

- [ ] **Step 2: FAIL** — Run: `npm test -w @kreiseck/uid`.

- [ ] **Step 3: Implementierung `fon.ts`**
```ts
import { buildEnvelope, callSoap, findDescendant, childText, type Session, type TransportOptions } from '@kreiseck/finanzonline-core';
import { normalisiereUid } from './normalisieren';
import { UidEingabeError, type UidErgebnis, type KeinAntwortGrund } from './ergebnis';

const ENDPOINT = 'https://finanzonline.bmf.gv.at/fonuid/ws/uidAbfrage/';
const NS = 'https://finanzonline.bmf.gv.at/fon/ws/uidAbfrage';

const TRANSIENT: Record<number, KeinAntwortGrund> = { [-2]: 'wartung', 12: 'ms_nicht_erreichbar', 1511: 'ms_nicht_erreichbar', 1512: 'ueberlast' };
const RATENLIMIT = new Set([1513, 1514]);
const NICHT_BER = new Set([10, 11, -4, 103, 104, 105]);

export async function fonUidAbfrage(args: { session: Session; antragsteller: string; uid: string; stufe: 1|2; transport?: TransportOptions }): Promise<UidErgebnis> {
  const ziel = normalisiereUid(args.uid);
  const tn = normalisiereUid(args.antragsteller);
  const datum = new Date().toISOString();
  const body = buildEnvelope({ namespace: NS, bodyElement: 'uidAbfrageServiceRequest', fields: [
    { name: 'tid', value: args.session.tid }, { name: 'benid', value: args.session.benid }, { name: 'id', value: args.session.id },
    { name: 'uid_tn', value: tn.voll }, { name: 'uid', value: ziel.voll }, { name: 'stufe', value: String(args.stufe) },
  ] });
  const root = await callSoap({ endpoint: ENDPOINT, soapAction: 'uidAbfrage', body }, args.transport);
  const resp = findDescendant(root, 'uidAbfrageServiceResponse');
  const rcText = resp ? childText(resp, 'rc') : undefined;
  const rc = Number.parseInt(rcText ?? '', 10);
  const base = { quelle: 'fon' as const, uid: ziel.voll, land: ziel.land, abfragedatum: datum, rohRc: String(rc) };
  if (rc === 0) {
    const erg: UidErgebnis = { ...base, ergebnis: 'gueltig' };
    const name = resp && childText(resp, 'name'); if (name) erg.name = name;
    const adr = resp ? ['adrz1','adrz2','adrz3','adrz4','adrz5','adrz6'].map((k) => childText(resp, k)).filter(Boolean).join(', ') : '';
    if (adr) erg.adresse = adr;
    erg.nachweis = { art: 'fon-bescheid-in-databox', datum, hinweis: 'Bescheid liegt am Folgetag in der DataBox (§132 BAO)' };
    return erg;
  }
  if (rc === 1) return { ...base, ergebnis: 'ungueltig' };
  if (rc === 4 || rc === 5) throw new UidEingabeError(`FON-UID rc ${rc}: ${resp && childText(resp, 'msg') || 'ungültige Eingabe'}`);
  if (RATENLIMIT.has(rc)) return { ...base, ergebnis: 'keine_antwort', grund: 'ratenlimit', wiederholbar: false };
  if (NICHT_BER.has(rc)) return { ...base, ergebnis: 'keine_antwort', grund: 'nicht_berechtigt', wiederholbar: false };
  const grund = TRANSIENT[rc];
  if (grund) return { ...base, ergebnis: 'keine_antwort', grund, wiederholbar: true };
  return { ...base, ergebnis: 'keine_antwort', grund: 'technisch', wiederholbar: true };
}
```

- [ ] **Step 4: PASS** — Run: `npm test -w @kreiseck/uid`.
- [ ] **Step 5: Commit** — `git commit -am "feat(uid): FON-UID Abfrage (SOAP) mit Returncode-Mapping"`

---

## Task 8: `createUid` Factory + `cacheKey`

**Files:**
- Create: `packages/uid/src/uid.ts`, `packages/uid/src/uid.test.ts`

**Interfaces:**
- Consumes: `viesPruefe`, `viesBestaetige`, `viesStatus`, `fonUidAbfrage`, `normalisiereUid`.
- Produces (Spec): `createUid(config: UidConfig): Uid` mit `pruefe`, `bestaetige`, `viesStatus`, `fon.abfrage`, `cacheKey`. `pruefe` = VIES-first (delegiert an `viesPruefe`). `fon.abfrage` erfordert `config.session` (sonst `Error`).
```ts
interface UidConfig { antragsteller: string; session?: Session; transport?: TransportOptions; viesBasis?: string; }
```

- [ ] **Step 1: Failing test**
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createUid } from './uid';
const jsonFetch = (b: unknown) => (async () => new Response(JSON.stringify(b), { status: 200 })) as unknown as typeof fetch;

test('pruefe delegiert an VIES (VIES-first)', async () => {
  const uid = createUid({ antragsteller: 'ATU12345678', transport: { fetchImpl: jsonFetch({ isValid: true, userError: 'VALID' }) } });
  const erg = await uid.pruefe('DE136695976');
  assert.equal(erg.ergebnis, 'gueltig'); assert.equal(erg.quelle, 'vies');
});
test('cacheKey ist stabil und normalisiert', () => {
  const uid = createUid({ antragsteller: 'ATU12345678' });
  assert.equal(uid.cacheKey(' de 136695976 '), 'DE136695976');
});
test('fon.abfrage ohne Session wirft', async () => {
  const uid = createUid({ antragsteller: 'ATU12345678' });
  await assert.rejects(() => uid.fon.abfrage({ uid: 'ATU87654321', stufe: 1 }), /Session/);
});
```

- [ ] **Step 2: FAIL**, **Step 3: Implementierung `uid.ts`**
```ts
import type { Session, TransportOptions } from '@kreiseck/finanzonline-core';
import { viesPruefe, viesBestaetige, viesStatus, type ViesConfig } from './vies';
import { fonUidAbfrage } from './fon';
import { normalisiereUid } from './normalisieren';

export interface UidConfig { antragsteller: string; session?: Session; transport?: TransportOptions; viesBasis?: string; }

export function createUid(config: UidConfig) {
  const vcfg: ViesConfig = { basis: config.viesBasis, fetchImpl: config.transport?.fetchImpl };
  return {
    pruefe: (uid: string) => viesPruefe(uid, vcfg),
    bestaetige: (args: { uid: string; name?: string; strasse?: string; plz?: string; ort?: string }) =>
      viesBestaetige({ ...args, antragsteller: config.antragsteller }, vcfg),
    viesStatus: () => viesStatus(vcfg),
    fon: {
      abfrage: (args: { uid: string; stufe: 1 | 2 }) => {
        if (!config.session) throw new Error('fon.abfrage erfordert eine Session in UidConfig');
        return fonUidAbfrage({ session: config.session, antragsteller: config.antragsteller, uid: args.uid, stufe: args.stufe, transport: config.transport });
      },
    },
    cacheKey: (uid: string) => normalisiereUid(uid).voll,
  };
}
export type Uid = ReturnType<typeof createUid>;
```
Hinweis: `ViesConfig.fetchImpl` aus `config.transport?.fetchImpl` ableiten (core `TransportOptions` trägt `fetchImpl`). Falls core-`TransportOptions` das Feld anders benennt, beim Bau angleichen.

- [ ] **Step 4: PASS**, **Step 5: Commit** — `git commit -am "feat(uid): createUid Factory + cacheKey"`

---

## Task 9: `@kreiseck/uid` Exports + README + CHANGELOG

**Files:**
- Modify: `packages/uid/src/index.ts`; Create `packages/uid/README.md`; Modify `CHANGELOG.md`

- [ ] **Step 1: `index.ts`**
```ts
export { createUid, type Uid, type UidConfig } from './uid';
export { viesPruefe, viesBestaetige, viesStatus, type ViesConfig } from './vies';
export { fonUidAbfrage } from './fon';
export { normalisiereUid } from './normalisieren';
export { UidEingabeError, type UidErgebnis, type Ausgang, type Nachweis, type KeinAntwortGrund } from './ergebnis';
```
- [ ] **Step 2: README.md** — Kurzbeschreibung, Beispiel `createUid(...).pruefe('DE…')`, 3-Ausgänge-Modell, VIES-first/FON-sekundär, Nachweis, Hinweis auf offene Live-Verifikationspunkte.
- [ ] **Step 3: CHANGELOG.md** — Abschnitt `## @kreiseck/uid` → `### 0.1.0` mit Feature-Liste.
- [ ] **Step 4: Build+Test+Lint** — Run: `npm run build -w @kreiseck/uid && npm test -w @kreiseck/uid && npm run lint && npm run format:check` → alles grün.
- [ ] **Step 5: Commit** — `git commit -am "docs(uid): index-Exports, README, CHANGELOG"`

---

## Task 10: `@kreiseck/finanzonline` Paket-Gerüst

**Files:**
- Create: `packages/finanzonline/{package.json,tsconfig.json,tsconfig.test.json,NOTICE,README.md,src/index.ts}`

- [ ] **Step 1: package.json** — wie Task 1, aber `"name": "@kreiseck/finanzonline"`, `"description": "FinanzOnline DataBox und weitere Verfahren"`, dependency `@kreiseck/finanzonline-core ^0.1.2`. `src/index.ts` = `export {};`.
- [ ] **Step 2: tsconfig(.test).json + NOTICE** — von rksv kopieren.
- [ ] **Step 3: Build** — Run: `npm install && npm run build -w @kreiseck/finanzonline` → grün.
- [ ] **Step 4: Commit** — `git add packages/finanzonline && git commit -m "feat(finanzonline): Paket-Geruest"`

---

## Task 11: DataBox `liste` (getDatabox)

**Files:**
- Create: `packages/finanzonline/src/databox.ts`, `packages/finanzonline/src/databox.test.ts`

**Interfaces:**
- Consumes core: `buildEnvelope`, `callSoap`, `findDescendant`, `childText`, `type XmlNode`, `type Session`, `type TransportOptions`.
- Produces: `createDatabox(session: Session, opts?: { transport?: TransportOptions }): Databox`. `liste(args?: { erltyp?: string; von?: Date; bis?: Date }): Promise<DataboxEintrag[]>`. Endpoint `https://finanzonline.bmf.gv.at/fon/ws/databox`, Namespace `https://finanzonline.bmf.gv.at/fon/ws/databox`, SOAPAction `getDatabox`, Body `getDataboxRequest` Felder `tid,benid,id,erltyp,ts_zust_von?,ts_zust_bis?` (dateTime `YYYY-MM-DDThh:mm:ss`). Response `result[databoxListEntry]` mit Feldern aus Research.
```ts
interface DataboxEintrag { stnr?: string; name: string; anbringen: string; zrvon: string; zrbis: string; datbesch: string; erltyp: string; fileart: 'XML'|'PDF'; tsZust: string; applkey: string; filebez: string; gelesen: boolean; betreff?: string; }
```

- [ ] **Step 1: Failing test**
```ts
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createDatabox } from './databox';
import type { Session } from '@kreiseck/finanzonline-core';
const s: Session = { id: 'ABCDEFGHIJ1234567890', tid: 'ABCD1234', benid: 'benutzer1', async logout() {} };
const LISTE = `<S:Envelope xmlns:S="http://schemas.xmlsoap.org/soap/envelope/"><S:Body><ns:getDataboxResponse xmlns:ns="https://finanzonline.bmf.gv.at/fon/ws/databox"><rc>0</rc><result><name>ACME</name><anbringen>RKDB</anbringen><zrvon>2026-01-01</zrvon><zrbis>2026-01-31</zrbis><datbesch>2026-07-22</datbesch><erltyp>P</erltyp><fileart>XML</fileart><ts_zust>2026-07-22T03:25:37</ts_zust><applkey>KEY0000000001</applkey><filebez>protokoll.xml</filebez><status></status></result></ns:getDataboxResponse></S:Body></S:Envelope>`;

test('liste parst databoxListEntry inkl. gelesen-Flag', async () => {
  const db = createDatabox(s, { transport: { fetchImpl: (async () => new Response(LISTE, { status: 200 })) as unknown as typeof fetch } });
  const eintraege = await db.liste({ erltyp: 'P' });
  assert.equal(eintraege.length, 1);
  assert.equal(eintraege[0]?.anbringen, 'RKDB');
  assert.equal(eintraege[0]?.fileart, 'XML');
  assert.equal(eintraege[0]?.applkey, 'KEY0000000001');
  assert.equal(eintraege[0]?.gelesen, false);
});
```

- [ ] **Step 2: FAIL**, **Step 3: Implementierung** `databox.ts` (`liste`): Envelope bauen (Datumsfelder nur wenn `von`/`bis` gesetzt, Format `d.toISOString().slice(0,19)`), `callSoap`, `findDescendant('getDataboxResponse')`, alle `result`-Kinder mappen (`childrenNamed`-Helfer wie in rksv `antwort.ts`), `gelesen = status === '1'`. rc≠0 → `FonError` mit rc/msg werfen (core-Fehlertyp).
- [ ] **Step 4: PASS**, **Step 5: Commit** — `git commit -am "feat(finanzonline): DataBox liste (getDatabox)"`

---

## Task 12: DataBox `eintrag` (getDataboxEntry, Base64)

**Files:**
- Modify: `packages/finanzonline/src/databox.ts`; Create `packages/finanzonline/src/databox-eintrag.test.ts`

**Interfaces:**
- Produces: `eintrag(applkey: string): Promise<{ fileart: 'XML'|'PDF'; inhalt: Buffer }>`. SOAPAction `getDataboxEntry`, Body `getDataboxEntryRequest` `tid,benid,id,applkey`. Response `result` = Base64 → `Buffer.from(result, 'base64')`. `fileart` ist im Entry-Result nicht enthalten → Aufrufer kennt sie aus `liste`; hier zusätzlich Signatur `eintrag(applkey, fileart)` mit `fileart`-Durchreichung.

- [ ] **Step 1: Failing test** — Response mit `<result>PGE+dGVzdDwvYT4=</result>` (Base64 von `<a>test</a>`); assert `inhalt.toString('utf8') === '<a>test</a>'`.
- [ ] **Step 2: FAIL**, **Step 3: Implementierung** — analog `liste`; `Buffer.from(childText(resp,'result') ?? '', 'base64')`. JSDoc-Warnung: „Abruf markiert den Eintrag in der DataBox als gelesen."
- [ ] **Step 4: PASS**, **Step 5: Commit** — `git commit -am "feat(finanzonline): DataBox eintrag (getDataboxEntry, Base64)"`

---

## Task 13: `@kreiseck/finanzonline` Exports + README + CHANGELOG

**Files:**
- Modify: `packages/finanzonline/src/index.ts`; Create README; Modify `CHANGELOG.md`

- [ ] **Step 1: index.ts** — `export { createDatabox, type Databox, type DataboxEintrag } from './databox';`
- [ ] **Step 2: README** — Beispiel: DataBox listen + Eintrag abholen; Hinweis „Abruf = gelesen"; Fenster-Limits (31 Tage / 7 Tage); Beispiel UID-Bescheid abholen (Filter über `anbringen` — Code [offen], live bestätigen).
- [ ] **Step 3: CHANGELOG** — `## @kreiseck/finanzonline` → `### 0.1.0`.
- [ ] **Step 4: Build+Test+Lint** — `npm run build -w @kreiseck/finanzonline && npm test -w @kreiseck/finanzonline && npm run lint && npm run format:check` grün.
- [ ] **Step 5: Commit** — `git commit -am "docs(finanzonline): index, README, CHANGELOG"`

---

## Task 14: CI-Matrix + Root-Integration

**Files:**
- Modify: `.github/workflows/ci.yml`, Root `README.md`, `docs/design.md` (Status-Tabelle)

- [ ] **Step 1:** In `ci.yml` die Build/Test-Schritte um `@kreiseck/uid` und `@kreiseck/finanzonline` erweitern (analog zu core/rksv).
- [ ] **Step 2:** `docs/design.md` Paket-Tabelle: `@kreiseck/uid` = fertig, `@kreiseck/finanzonline` = DataBox (Teil), Rest später. Root-README kurz ergänzen.
- [ ] **Step 3: Voller Lauf** — `npm run build -w @kreiseck/finanzonline-core && npm run build -w @kreiseck/uid && npm run build -w @kreiseck/finanzonline && npm test -w @kreiseck/uid && npm test -w @kreiseck/finanzonline && npm run lint && npm run format:check` → alles grün.
- [ ] **Step 4: Commit** — `git commit -am "ci+docs: @kreiseck/uid und @kreiseck/finanzonline aufgenommen"`

---

## Nach der Umsetzung (separat, nicht Teil der Tasks)
- **Offene Live-Verifikation** (Spec §Offene Punkte) gegen VIES-Sandbox + FON-TEST: exakte VIES-POST-Feldnamen/`requestIdentifier`-Format, DE-Stufe-2-Leerfelder, `anbringen`-Code der UID-Bestätigung. Ergebnisse in Fixtures/Tests festschreiben; ggf. Feldnamen im Code angleichen.
- Optionaler VIES-SOAP-Pfad, FileUpload, weitere FON-Dienste als spätere Stufen.
- Veröffentlichung (`npm publish -w`) erst nach grüner Live-Verifikation.
